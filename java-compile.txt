Чтобы скомпилировать программу Java, необходимо вызвать компилятор

$ javac <имя модуля>.java

Результатом компиляции является байт-код <имя модуля>.class. Для запуска программы нужно вызвать

$ java <имя класса> <один или несколько аргументов>

Отметим, что расширение не нужно указывать